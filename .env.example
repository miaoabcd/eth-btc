# Environment variables (optional)
# - This app does NOT read env vars for config overrides.
# - RUST_LOG controls tracing output.
# - You may export secrets for convenience and pass them to CLI or config files manually.

RUST_LOG=info

# Optional for shell convenience (not read automatically)
# HYPERLIQUID_PRIVATE_KEY=0xYOUR_PRIVATE_KEY
# HYPERLIQUID_VAULT_ADDRESS=0xYOUR_VAULT
